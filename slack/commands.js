// const { openModal } = require('./modal');

// const handleCommand = async (req, res, client) => {
//   const { command } = req.body;
//   if (command === '/approval-test') {
//     await openModal(client, req.body.trigger_id);
//     res.status(200).send();
//   }
// };

// module.exports = { handleCommand };
